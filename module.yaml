format: toporealm.module/v2
id: workflow
namespace: wf
version: "1.0.0"
kinds:
  objects: [task, checkpoint, execution_report, settings]
  relations: [depends_on, fallback, iterates]
ui:
  color: "#3b82f6"
  icon: "checkbox"
  titleKey: title
entry: ./dist/index.js
