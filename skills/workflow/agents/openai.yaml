interface:
  display_name: "Workflow"
  short_description: "Route a TopoRealm workflow request and choose its next phase"
  default_prompt: "Use $workflow to route this work and identify the next workflow phase."
